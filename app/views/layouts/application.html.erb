<!DOCTYPE html>
<html>
<head>
  <title>Reffy</title>
 <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tag %>
</head>
<body>
<div class="container">
	<div class="row">
		<div id="header" class="twelve columns">
			<h1><a href="/">Reffy</a></h1>
		</div>
	</div>

<div class="row">
	<div id="navigation" class="twelve columns">
		<ul class="nav-bar" class="<%= @reporting ? "reporting" : "enquiries" %>">
			<li><%= link_to "Enquiries", enquiries_path, :class => "enquiries" %></li>
			<li><%= link_to "Reports", reporting_enquiries_path, :class => "reporting" %></li>
		</ul>
		</div>
		</div>
		<div class="row">
		<div id="content" class="eight columns">
		<%= yield %>
	</div>
	
	<div class="four columns">
		<div class="key">
		<h4>Input Key</h4>
		*enquiry type
		<br />
		#subject
		<br />
		@client
		<h4>Time Formats</h4>
		30 minutes
		<br />
		1.5 hours, 1 hour
		<br />
		1 day (8 hours)
		</div>
		<h3>Recent Subjects</h3>
		<ul class="subjects">
		<% Enquiry.project_counts.each do |project| %>
			<li><a href="<%= reporting_enquiries_path(:project => project.name) %>" class="project important"><%= project %></a> (<%= hours_for(project) %>)</li>
		<% end %>
		</ul>
		
		<h3>Recent Clients</h3>
		<ul class="projects">
		<% Enquiry.client_counts.each do |client| %>
			<li><a href="<%= reporting_enquiries_path(:client => client.name) %>" class="client important"><%= client %></a> (<%= hours_for(client) %>)</li>
		<% end %>
		</ul>
		
		<h3>Recent Actions</h3>
		<ul class="projects">
		<% Enquiry.action_counts.each do |action| %>
			<li><a href="<%= reporting_enquiries_path(:act => action.name) %>" class="action important"><%= action %></a> (<%= hours_for(action) %>)</li>
		<% end %>
		</ul>
	</div>
</div>
	<div class="grid_12" id="footer">
		Based on Punchy which was created by <a href="http://www.mazondo.com">Ryan Quinn</a>
		<br />
		<a href="http://www.opensourcerails.com/projects/607989-Punchy" title="Open Source Rails">See Punchy on Open Source Rails</a> | <a href="http://www.github.com/katel/Reffy">Reffy on Github</a>
		
	</div>
</div>

</body>
</html>
